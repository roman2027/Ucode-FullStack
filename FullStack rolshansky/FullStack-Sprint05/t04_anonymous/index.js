const i = {
    getAnonymous: (name, alias, affiliation) => {
        return {
            name: name,
            alias: alias,
            affiliation: affiliation
        }
    }
}

module.exports = i;